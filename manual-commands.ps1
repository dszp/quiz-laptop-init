# Run in an Administrative PowerShell prompt manually, this command allows all PowerShell to run:
Set-Execution-Policy -ExecutionPolicy Unrestricted

# Run in an Administrative PowerShell prompt manually against a specific file, this unblocks downloaded files to execute:
Unblock-File FILEHERE
